import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
  },
})
